import Foundation

// MARK: - Data Entry Components (P1+)
// Scope: Button, Input, Textarea, Checkbox, RadioGroup, Switch, Select, Field, etc.

public enum DataEntrySectionMarker {
    public static let sectionName = "DataEntry"
}
